
// ES6 introduced the let and const keywords, which are block-scoped.
// This means that the variable is only available within the block in which it is declared.

// The var keyword is function-scoped, which means that the variable is only available within the function in which it is declared.

// The let and const keywords are block-scoped, which means that the variable is only available within the block in which it is declared.